machine:
            
    environment:
        DATABASE_URL: postgres://postgres@127.0.0.1:5433/rha
             
database:
    override:
        - postgres
             
dependencies:
    pre:
        - npm install
    postgres:
        version:
            9.5.5
        
test:
    override:
        - npm bin
        - ./node_modules/.bin/newman --version
        
        
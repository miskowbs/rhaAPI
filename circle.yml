machine:
        
    environment:
        DATABASE_URL: postgres://postgres@127.0.0.1:5433/rha
             
dependencies:
    pre:
        - npm install
    tags:
        - linux
        - node
        - postgres
        
test:
    override:
        - newman run ./newmanTests/committees.json -e ./newmanTests/postmanEnvironment.json
        
        
machine:
        
    postgres:
        version:
            9.5.5
     
dependencies:
    pre:
        - npm install     
        
test:
    override:
        - newman run ./newmanTests/committees.json -e ./newmanTests/postmanEnvironment.json
        
        
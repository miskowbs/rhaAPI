machine:
        
    environment:
        DATABASE_URL: postgres://postgres@127.0.0.1:5433/rha
             
dependencies:
    pre:
        - npm install
        
test:
    override:
        - npm test
        
        